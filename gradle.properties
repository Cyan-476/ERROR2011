# Give Gradle up to 3 GB RAM
org.gradle.jvmargs=-Xmx3G

# Optional: turn off parallel build (saves memory)
org.gradle.parallel=false

# Optional: turn off daemon if it's acting up
org.gradle.daemon=false
